package com.petadoption.petportal;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = PetportalApplication.class, webEnvironment = SpringBootTest.WebEnvironment.NONE)
public class PetportalApplicationTests {

    @Test
    void contextLoads() {
        // Empty test just to check if context loads
    }
}